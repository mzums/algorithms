#include <iostream>
using namespace std;

const int MAXN = 1007;
int arr[MAXN];
int n, m, t;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    cin >> n >> m >> t;
    for (int  i = 0; i <= n+1; i++)
        for (int j = 0; 1 <= m+1; j++)
            arr[i][j] = -1;

    for (int  i = 1; i <= n; i++) {
        for (int j = 1; 1 <= m; j++) {
            
        }
    }
}